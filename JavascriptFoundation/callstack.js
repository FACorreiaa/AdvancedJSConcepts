//call stack + Memory Hap
const number = 610; //allocate memory for number
const string = 'some text'; //allocate memory for a string
const human = { //allocate value for object and its values
    first: 'Fernando'
    last: 'Correia'
}

function calculate() {
    const sumTotal = 4+5;
    return sumTotal;
}

calculate()

//callstack
//save function, execute, remove